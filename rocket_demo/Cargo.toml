[package]
edition = "2021"
name = "rocket_demo"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
logger = {git = "https://github.com/yuexclusive/rutils", package = "time"}
rocket = {version = "0.5.0-rc.2", features = ["secrets", "json"]}
